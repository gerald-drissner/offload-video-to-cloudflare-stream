{
  "name": "voffload/voffload-cloudflare-stream",
  "description": "Upload WordPress video attachments to Cloudflare Stream and serve them through the Cloudflare Stream player.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.1"
  },
  "scripts": {
    "lint:php": "find voffload-cloudflare-stream -name '*.php' -print0 | xargs -0 -n1 php -l"
  }
}
